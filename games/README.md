## Histórico de partidos

En esta carpeta encontrarás todos los partidos que la web de horarios de Movistar NBA ha ido anunciando. Hay una Github Action que se ejecuta a diario que parsea la web y que añade los partidos al fichero `historical.json`. Ese fichero no se carga desde la web, así que para eso se construye el `schedule.json`.
